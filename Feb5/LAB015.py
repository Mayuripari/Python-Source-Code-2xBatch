# string immutability- string are immutable, we can replace the value but cant change
# Immutable means that cant be changed or modify
name = "Hello"
# name[0] = 'p'       # TypeError: 'str' object does not support item assignment
name = "Mayuri"
print(name)
